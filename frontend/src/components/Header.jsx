import React from 'react';

const Header = () => {
    return (
        <header>
            <button>About Us</button>
            <button>Contact</button>
        </header>
    );
};

export default Header;
